name: CI

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  clippy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: ${{ runner.os }}-cache-v1
          cache-on-failure: 'true'
          cache-all-crates: 'true'

      - name: Install Rust components
        run: rustup component add rustfmt clippy

      - name: Formatting
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --locked --all-features

  build:
    runs-on: ubuntu-latest
    needs: clippy

    steps:
      - uses: actions/checkout@v5

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: ${{ runner.os }}-cache-v1
          cache-on-failure: 'true'
          cache-all-crates: 'true'

      - name: Log into Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Compile
        run: |
          cargo build --locked -rF production
          ls -la target

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/sanke'

    steps:
      - name: Create deployment request
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo '{
            "ref": "${{ github.sha }}",
            "environment": "production",
            "auto_merge": false,
            "required_contexts": ["build"],
            "payload": {
              "deployment_name": "aaf-app",
              "container_name": "aaf-app",
              "image": "ghcr.io/${{ github.repository }}:sanke"
            }
          }' | gh api repos/${{ github.repository }}/deployments --method POST --input -
