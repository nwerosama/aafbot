[package]
name = "aaf_shared"
description = "Share reusable stuff between crates"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
asahi = { workspace = true }
sqlx = { workspace = true }
